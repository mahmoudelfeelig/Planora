This section reports empirical results obtained by running the released code.
We recommend reporting both feasibility/performance and schedule quality.

\subsection{Generated Instances}
We evaluate on generated instances produced by the project's generator modes (e.g., \texttt{small\_demo}, \texttt{target\_case}).
For each mode and seed, we record:
\begin{itemize}[leftmargin=*]
  \item number of programs/groups/courses/activities/rooms/staff,
  \item solver mode (\texttt{cp\_rooms} vs \texttt{greedy}),
  \item CP-SAT time limit, number of workers, objective on/off,
  \item feasibility status and runtime,
  \item soft penalty before/after local search.
\end{itemize}

The runs in Table~\ref{tab:perf} were produced by \texttt{scripts/run\_experiments.py}
with seed \(1\), time limit \(120\) seconds, \(4\) workers, CP objective enabled, and
local-search budget \texttt{ls\_iters=2000}, \texttt{ls\_seconds=15}. The generated JSONL
output was converted to LaTeX rows using \texttt{scripts/jsonl\_to\_latex\_table.py}.

On \texttt{small\_demo}, both rooming modes reached \texttt{OPTIMAL}; \texttt{cp\_rooms}
obtained a lower penalty than \texttt{greedy} at higher CP runtime. On \texttt{target\_case},
both modes returned \texttt{UNKNOWN} within the 120-second limit, so no feasible schedule was
extracted and penalty values are reported as ``-''.

\subsection{Public Benchmarks (Optional)}
If using public benchmarks (e.g., International Timetabling Competition instances~\cite{itc2007_track3}), we must define a mapping from benchmark input to this paper's data model and constraints.
Only benchmarks that can be mapped without altering the stated constraints should be reported.

\subsection{Tables and Figures}
Table~\ref{tab:perf} reports the measured runtime and quality metrics from these runs.

\begin{table}[h]
\centering
\begin{tabular}{lrrrrr}
\toprule
Scenario & \#Acts & Mode & CP time (s) & Penalty (base) & Penalty (LS) \\
\midrule
\input{sections/07_experiments_table.tex}
\bottomrule
\end{tabular}
\caption{Performance and quality on generated instances (\texttt{-} means no feasible schedule extracted within the time limit).}
\label{tab:perf}
\end{table}
